:javascript
  window.incomingUrl = '#{@url}';
  window.App.codemark = new App.Codemark(#{@codemark.to_json});
  window.App.topics = new App.Topics(#{@topics.to_json});
  window.App.init();

#codemarklet
