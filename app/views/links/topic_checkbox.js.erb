var chb = $("#topic_chb_<%= @topic.slug %>");
console.log(chb)
if (chb.length == 0) {
  $("#topic_tags").append("<%= escape_javascript(render '/codemarks/topic_tag_item', :checked => true, :topic => @topic) %>");
} else {
  chb.closest("li").effect("highlight", {}, 2000);
  //chb.closest("li").toggleClass("highlight", 1000).animate(1000);
    //.switchClass("","highlight", 5000)
    //.switchClass("highlight","", 5000);//.animate({ .unflash}, 1500);
}
$("#link_form_topic_autocomplete").val("");
$("#link_form_topic_slug").val("");
prepareTagDeletes();
