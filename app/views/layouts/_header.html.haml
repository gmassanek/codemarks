%script
  :plain
    window.USER_ID = #{current_user.id if current_user}
#header
  .site_content
    #logo
      - logo = image_tag 'cm-logo-1-small.png'
      = link_to logo, root_path

    = render '/codemarks/form' if logged_in?
    = link_to 'Add Text codemark', '#', :id => 'add_text_codemark'
    = render '/layouts/authentication'
