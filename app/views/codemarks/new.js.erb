form_html = "<%= escape_javascript(render '/codemarks/full_form') %>"
$('#codemark_form').replaceWith(form_html)

CodemarkForm.bootstrap();

$(document).keyup(function(event) {
  if (event.keyCode == 27) {
    $('#codemark_form').replaceWith("<%= escape_javascript(render '/codemarks/form') %>")
  }
});
