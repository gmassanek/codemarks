$('#codemarks').replaceWith("<%= escape_javascript(render('/shared/codemarks')) %>");
$('#filters_and_sorts').replaceWith("<%= escape_javascript(render('/users/codemark_filters', tabs: false)) %>");

style_lists();
prepareCodemarks();
